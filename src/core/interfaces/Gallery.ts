export interface SmileTransformation {
    id: string;
    beforeUrl: string;
    afterUrl: string;
    category: string;
    title: string;
    description: string;
}

export interface GalleryImage {
    id: string;
    url: string;
    alt: string;
}
